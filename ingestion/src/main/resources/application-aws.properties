# No HTTP server needed — the app is driven entirely by SQS events
spring.main.web-application-type=none

# SQS queue that receives S3 ObjectCreated notifications
app.sqs.queue-name=${SQS_QUEUE_NAME:rag-document-queue}
